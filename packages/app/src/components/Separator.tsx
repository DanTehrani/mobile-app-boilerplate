import colors from '@/lib/styles/colors';
import { View } from 'react-native';

const Separator = () => {
  return <View style={{ height: 1, backgroundColor: colors.subbedText }} />;
};

export default Separator;
